package com.devicesyncagnt

import android.content.Context

object FileUploader {
    fun uploadFile(context: Context, filePath: String) {
        // Upload logic via Retrofit or HttpURLConnection
    }
}