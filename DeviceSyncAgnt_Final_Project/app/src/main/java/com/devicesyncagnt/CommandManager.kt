package com.devicesyncagnt

import android.content.Context

object CommandManager {
    fun sync(context: Context) {
        // Call command API, parse response, trigger action (get_camera, send_gallery, etc.)
    }
}