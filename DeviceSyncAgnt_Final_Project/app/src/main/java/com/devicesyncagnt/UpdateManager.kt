package com.devicesyncagnt

import android.content.Context

object UpdateManager {
    fun checkForUpdate(context: Context) {
        // Download APK and trigger silent install (if permissions granted)
    }
}